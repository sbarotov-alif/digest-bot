# 📖 Инструкция по запуску Digest Bot

## Что делает бот
Каждый день в 19:00 (Ташкент) бот читает 7 Telegram-каналов,
фильтрует посты по ключевым словам про банки/кредиты/бизнес
и отправляет дайджест в ваш канал.

---

## Шаг 1 — Загрузите файлы на GitHub

1. Зайдите на https://github.com и войдите в аккаунт
2. Нажмите кнопку **"New repository"** (зелёная кнопка)
3. Название: `digest-bot`, настройки оставьте по умолчанию → **Create repository**
4. На следующей странице нажмите **"uploading an existing file"**
5. Перетащите все 4 файла: `bot.py`, `requirements.txt`, `railway.toml`, `.gitignore`
6. Нажмите **Commit changes**

---

## Шаг 2 — Первый запуск (получение сессии Telethon)

Это нужно сделать ОДИН РАЗ на вашем компьютере, чтобы авторизоваться в Telegram.

### Установите Python (если нет):
Скачайте с https://python.org/downloads — версия 3.11 или выше

### Откройте терминал/командную строку и выполните:

```bash
pip install telethon
python -c "
from telethon.sync import TelegramClient
import os
api_id = input('Введите api_id: ')
api_hash = input('Введите api_hash: ')
client = TelegramClient('session_digest', int(api_id), api_hash)
client.start()
print('✅ Сессия создана! Файл session_digest.session появился в папке.')
client.disconnect()
"
```

После этого в папке появится файл `session_digest.session`.

### Загрузите этот файл на GitHub:
Зайдите в ваш репозиторий → **Add file** → загрузите `session_digest.session`

---

## Шаг 3 — Запустите на Railway

1. Зайдите на https://railway.app
2. Нажмите **"New Project"** → **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий `digest-bot`
4. Railway начнёт сборку — подождите 1-2 минуты

### Добавьте переменные окружения:
В Railway откройте ваш проект → вкладка **Variables** → добавьте:

| Имя переменной | Значение |
|---|---|
| `API_ID` | ваш api_id с my.telegram.org |
| `API_HASH` | ваш api_hash |
| `BOT_TOKEN` | токен от @BotFather |
| `TARGET_CHANNEL` | @username вашего канала |

5. После добавления переменных Railway перезапустит бота автоматически

---

## Шаг 4 — Тест

Чтобы проверить что всё работает — в файле `bot.py` найдите строку:
```python
# run_digest()
```
Уберите `#` (раскомментируйте), сохраните и загрузите на GitHub.
Бот сразу отправит дайджест. Потом верните `#` обратно.

---

## Ключевые слова (можно изменить в bot.py)

Текущий список в переменной `KEYWORDS`:
- банк, кредит, бизнес, b2b
- рассрочка, bnpl
- кредитная карта
- tbc, alif, anorbank, ipotekabank

---

## Каналы для мониторинга (можно добавить в bot.py)

Список в переменной `SOURCE_CHANNELS`. Добавьте username канала без @.

---

## Частые вопросы

**Бот не отправляет?**
- Проверьте что бот добавлен администратором в ваш канал
- Проверьте правильность TARGET_CHANNEL (с @ или без — попробуйте оба варианта)

**Ошибка авторизации Telethon?**
- Повторите Шаг 2 заново

**Хочу другое время?**
- В `bot.py` измените `SEND_TIME_UTC = "14:00"` (это 19:00 по Ташкенту)
- Формула: нужное время Ташкент минус 5 часов = UTC

---

## Поддержка

Если что-то не работает — сделайте скриншот ошибки в Railway (вкладка Logs)
и обратитесь за помощью.
